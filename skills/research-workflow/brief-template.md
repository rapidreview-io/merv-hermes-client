<!--
  Task brief — RENDERED BY MERV, not authored.

  The goal and deliverables are fixed at task.create and are immutable;
  Merv renders this file from them and pins it as tasks/<name>/brief.md
  (role "brief"). Submitting a brief against a task is refused. This file
  documents the rendered shape so reviewers and tools know what to expect.

  Writing the goal (at task.create):
    goal          2-4 sentences — what needs to be done and why, STANDALONE:
                  a person just opening the task understands it. Name
                  concrete datasets, tools, and experiments; never "the
                  wave" or "this reflection". No method.
    deliverables  one item per thing that must exist when the task is done,
                  each verifiable AS WRITTEN — carry the criterion in the
                  sentence (counts, tolerances, required sections). No
                  bundles, no vague nouns. Rule of thumb: 1-7 items.
-->

# Brief: <task name>

## Goal
<goal prose>

## Deliverables
1. <deliverable — verifiable as written>
2. …
