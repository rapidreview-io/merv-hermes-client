# Connect Merv

Run:

```bash
hermes mcp add merv --url https://experiments.rapidreview.io/mcp --auth oauth
```

Complete the browser sign-in, then start a new Hermes session. When Merv
announces an update, run `hermes plugins update merv`.
